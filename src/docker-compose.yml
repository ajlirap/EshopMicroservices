services:
  catalogdb:
   image: postgres
  basketdb:
   image: postgres

  catalogapi:
    image: ${DOCKER_REGISTRY-}catalogapi
    build:
      context: ..
      dockerfile: src/Catalog/CatalogApi/Dockerfile

volumes:
  postgres_catalog:
  postgres_basket:


